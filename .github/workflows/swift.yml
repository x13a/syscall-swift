name: Swift

on:
  push:
    branches: [ master ]
    paths:
      - 'Sources/**'
      - 'Tests/**'
      - 'Package.swift'
      - '.github/workflows/swift.yml'
  pull_request:
    branches: [ master ]
    paths:
      - 'Sources/**'
      - 'Tests/**'
      - 'Package.swift'
      - '.github/workflows/swift.yml'

jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v7
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
